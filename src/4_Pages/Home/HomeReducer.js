const initialState = {
  boardList: [],
  selectedBoard: null,
};

const TodoReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default TodoReducer;
